server:
  port: 8083

spring:
  datasource:
    # driverClassName: "org.postgresql.Driver"
    # url: "jdbc:postgresql://127.0.0.1:5432/YAHOO_FINANCE"
    # username: "postgres"
    # password: "admin1234"
    driverClassName: "com.mysql.jdbc.Driver"
    url: "jdbc:mysql://127.0.0.1:3306/BOOTCAMP_2408"
    username: "root"
    password: "admin1234"
  jpa:
    show-sql: true # this line shows the SQL executed in the terminal
    hibernate.ddl-auto: create

# Custom variables
api:
  yf:
    domain: "query1.finance.yahoo.com"
    endpoints: "/v7/finance/quote"
